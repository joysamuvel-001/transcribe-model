# ============================================================
# MedTranscribe — Python Dependencies
# Python 3.10  |  conda env: nemotron310
# ============================================================

# ── Web Framework ────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9          # required for File/Form uploads

# ── Audio I/O ────────────────────────────────────────────────
soundfile==0.12.1
scipy==1.13.0
av==12.1.0                       # PyAV — WebM/Opus → WAV (bundles its own ffmpeg)

# ── NVIDIA NeMo ──────────────────────────────────────────────
# Installs NeMo ASR + TitaNet + Sortformer
nemo_toolkit[asr]==1.23.0

# ── PyTorch (CPU fallback — GPU users see note below) ────────
torch==2.2.2
torchaudio==2.2.2

# ── ML Utilities ─────────────────────────────────────────────
numpy==1.26.4
scikit-learn==1.4.2              # cosine similarity in registry.py

# ── Misc ─────────────────────────────────────────────────────
huggingface-hub==0.23.2          # model downloads from HuggingFace
omegaconf==2.3.0                 # NeMo config system
hydra-core==1.3.2

# ============================================================
# GPU NOTE (CUDA 12.1):
# Replace the torch lines above with:
#
#   pip install torch==2.2.2 torchaudio==2.2.2 \
#       --index-url https://download.pytorch.org/whl/cu121
#
# ============================================================